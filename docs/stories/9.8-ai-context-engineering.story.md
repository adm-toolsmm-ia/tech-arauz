# Story 9.8 — AI Context Engineering: toAIContext() + Visualização Rica de Documentação

**EPIC:** 9 — Org Management UX: Navegação Hierárquica Contextual
**Story ID:** 9.8
**Status:** Draft
**Sprint:** Sprint 5
**Assignee:** @dev (Dex)
**Priority:** MÉDIA — Preparação para EPIC 7.4

---

## Objetivo

Preparar os dados estruturados de Organização para alimentar o Chatbot AI (EPIC 7.4). Criar transformer `toAIContext()` que converte entidades OrgProcess, OrgRoutine, OrgActivity em formato plano, serializado, otimizado para tokenização. Melhorar visualização de campos JSONB de documentação com destaque e semântica.

---

## Critérios de Aceitação

- [ ] `src/lib/transformers/organization.ts` criado
  - Função `toAIContext(entity: OrgProcess | OrgRoutine | OrgActivity): OrgAIContext`
  - Retorna objeto plano (sem nesting profundo):
    ```typescript
    interface OrgAIContext {
      entityType: 'process' | 'routine' | 'activity';
      entityId: string;
      title: string;
      objective: string | null;
      description: string | null;
      roles: string[];
      procedures: string | null;
      instructions: string | null;
      steps: string[];          // steps[] numerados — easy tokenization
      rules: string | null;      // campo `regra` — decisory weight for AI
      deadline: string | null;   // prazo
      executionLimit: string | null; // horario_limite
      inputs: Array<{ name: string; description?: string; required: boolean }>;
      outputs: Array<{ name: string; description?: string }>;
      risks: Array<{ name: string; severity?: string }>;
      impacts: Array<{ name: string; description?: string }>;
      avgExecutionTime: string | null; // formatted like "45 min", "2h 30m"
      complexity?: 'low' | 'medium' | 'high'; // Activity only
      priority?: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'; // Activity only
    }
    ```
  - Função genérica para qualquer entidade (polymorphic)
  - Zero server calls — apenas transformação local
  - Export: `export function toAIContext(entity: OrgProcess | OrgRoutine | OrgActivity): OrgAIContext`
- [ ] **Melhorias de UI em DocumentationAccordion:**
  - Campo `regra` com destaque **especial:**
    - Ícone `<AlertCircle>` em vermelho (destructive)
    - Text em `text-destructive/80`
    - Background subtle `bg-destructive/5`
    - Prefix visual: `[REGRA DE NEGÓCIO]` em bold
  - Campo `prazo` com layout lado-a-lado:
    - Ícone `<Clock>` em primary
    - Layout: "Prazo: {prazo}" | "Limite: {horario_limite}"
  - Steps (se exitem):
    - Renderiza como `<ol>` numerada (não bullet)
    - Styling: numeração bold, gap-3 entre items
    - Cada step é `<li>` com text-sm
  - Campos faltando (null) não renderizam (AccordionItem não existe)
- [ ] **Rutes suportadas:**
  - `export { toAIContext } from '@/lib/transformers/organization'`
  - Usável em componentes e Server Actions
  - Type-safe: TypeScript infere tipo retornado
- [ ] **Documentação:**
  - Adicionar JSDoc a `toAIContext` com exemplo de uso
  - Incluir campo de exemplo OrgAIContext no arquivo
  - Notas sobre tokenização (steps são strings separadas para fácil split)
- [ ] **Integração futura (preparado para EPIC 7.4):**
  - Chatbot AI pode chamar `toAIContext(process)` antes de gerar resposta
  - Sistema de prompt pode incluir: `AI_CONTEXT: ${JSON.stringify(toAIContext(process))}`
  - Rules (campo `regra`) marcadas para ênfase em resposta AI
- [ ] Build passes (`npm run build`)
- [ ] TypeCheck passes (`npm run typecheck`)

---

## File List

**Criados:**
- `src/lib/transformers/organization.ts` — nova pasta/arquivo

**Refatorados (referência):**
- `src/components/organization/DocumentationAccordion.tsx` — destaque regra/prazo (já feito em Story 9.5)
- `src/types/organization.ts` — adicionar `OrgAIContext` interface

**Referência (não modificar):**
- `src/components/organization/ActivityCockpit360.tsx` (Story 9.5)
- `src/app/actions/organization.ts`

---

## Dependências

**Internas:**
- Story 9.5 (ActivityCockpit360 — prepara dados estruturados)

**Externas:**
- Nenhuma (pode rodar antes/depois de outras stories)

**Bloqueada por:**
- 9.5 ✓ (precisa ActivityCockpit para contexto)

**Bloqueia:**
- EPIC 7.4 (Chatbot AI — vai usar toAIContext)

---

## Notas Técnicas

### toAIContext Implementation

```typescript
// src/lib/transformers/organization.ts

export interface OrgAIContext {
  entityType: 'process' | 'routine' | 'activity';
  entityId: string;
  title: string;
  objective: string | null;
  description: string | null;
  roles: string[];
  procedures: string | null;
  instructions: string | null;
  steps: string[];
  rules: string | null;
  deadline: string | null;
  executionLimit: string | null;
  inputs: Array<{ name: string; description?: string; required: boolean }>;
  outputs: Array<{ name: string; description?: string }>;
  risks: Array<{ name: string; severity?: string }>;
  impacts: Array<{ name: string; description?: string }>;
  avgExecutionTime: string | null;
  complexity?: 'low' | 'medium' | 'high';
  priority?: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL';
}

/**
 * Transforma entidade de organização em contexto plano para AI.
 *
 * @param entity - OrgProcess, OrgRoutine, ou OrgActivity
 * @returns OrgAIContext — objeto plano, sem nesting, serializable
 *
 * @example
 * const process = await getProcess(id);
 * const context = toAIContext(process);
 * const prompt = `Baseado neste processo: ${JSON.stringify(context)}...`;
 */
export function toAIContext(
  entity: OrgProcess | OrgRoutine | OrgActivity
): OrgAIContext {
  const documentation = entity.documentation || {};
  const inputs = (entity as OrgProcess)?.inputs || [];
  const outputs = (entity as OrgProcess)?.outputs || [];
  const risks = (entity as OrgActivity)?.risks || [];
  const impacts = (entity as OrgActivity)?.impacts || [];

  return {
    entityType:
      'average_execution_time' in entity ? 'activity' :
      'activities_count' in entity ? 'routine' :
      'process',
    entityId: entity.id,
    title: entity.name,
    objective: 'objective' in entity ? entity.objective : null,
    description: entity.description || null,
    roles: entity.responsible_roles || [],
    procedures: documentation.procedures || null,
    instructions: documentation.instructions || null,
    steps: Array.isArray(documentation.steps) ? documentation.steps : [],
    rules: documentation.regra || null,
    deadline: documentation.prazo || null,
    executionLimit: documentation.horario_limite || null,
    inputs: inputs.map(i => ({
      name: i.name,
      description: i.description,
      required: i.required || false
    })),
    outputs: outputs.map(o => ({
      name: o.name,
      description: o.description
    })),
    risks: risks.map(r => ({
      name: r.name,
      severity: r.severity
    })),
    impacts: impacts.map(im => ({
      name: im.name,
      description: im.description
    })),
    avgExecutionTime: ('average_execution_time' in entity)
      ? formatExecutionTime(entity.average_execution_time)
      : null,
    ...(('complexity' in entity) && { complexity: entity.complexity }),
    ...(('priority' in entity) && { priority: entity.priority })
  };
}

function formatExecutionTime(minutes: number | null): string | null {
  if (!minutes) return null;
  if (minutes < 60) return `${minutes} min`;
  const hours = Math.floor(minutes / 60);
  const mins = minutes % 60;
  return mins > 0 ? `${hours}h ${mins}m` : `${hours}h`;
}
```

### DocumentationAccordion — Campo Regra

```typescript
// Em DocumentationAccordion.tsx (já feito em 9.5, mas detalhe aqui)

if (data.regra) {
  <AccordionItem value="regra" className="border-l-2 border-destructive">
    <AccordionTrigger className="hover:text-destructive">
      <AlertCircle className="w-4 h-4 text-destructive mr-2" />
      <span className="font-bold text-destructive">REGRA DE NEGÓCIO</span>
    </AccordionTrigger>
    <AccordionContent className="bg-destructive/5 text-destructive/80">
      {data.regra}
    </AccordionContent>
  </AccordionItem>
}
```

### Uso em EPIC 7.4 (Chatbot AI)

```typescript
// No Chatbot AI component:
import { toAIContext } from '@/lib/transformers/organization';

const handleAsk = async (question: string, context: OrgProcess) => {
  const aiContext = toAIContext(context);

  const prompt = `
Você é um assistente especializado em processos organizacionais.

CONTEXTO DO PROCESSO:
${JSON.stringify(aiContext, null, 2)}

REGRAS CRÍTICAS (se aplicável):
${aiContext.rules ? `- ${aiContext.rules}` : 'Nenhuma regra específica.'}

PERGUNTA DO USUÁRIO: ${question}

Responda de forma clara e relacionada ao processo acima.
  `;

  const response = await askClaude(prompt);
  return response;
};
```

---

## CodeRabbit Integration

**Areas de Review Esperadas:**
- Type safety (discriminated union de entity types)
- Null handling (campos documentação podem ser null)
- Array mapping (inputs/outputs/risks/impacts transformation)
- Tokenization friendliness (steps como array separado)
- JSDoc completeness
- DocumentationAccordion styling consistency

**Auto-Healing Config:**
```yaml
prompts:
  - "Ensure toAIContext handles null/undefined fields gracefully"
  - "Verify steps[] is separate array (easy for AI tokenization)"
  - "Check rules field is marked for AI emphasis via JSDoc"
  - "Ensure DocumentationAccordion 'regra' has destructive color and border-left"
  - "Verify formatExecutionTime returns readable strings (45 min, 2h 30m)"
  - "Check TypeScript entity type discrimination works correctly"
```

---

## Tasks

- [ ] Criar `src/lib/transformers/organization.ts`
- [ ] Definir interface `OrgAIContext` com todos campos
- [ ] Implementar função `toAIContext(entity)` polimórfica
- [ ] Implementar `formatExecutionTime(minutes)` helper
- [ ] Adicionar JSDoc completo com exemplo
- [ ] Adicionar interface `OrgAIContext` a `src/types/organization.ts`
- [ ] Refatorar `DocumentationAccordion.tsx` — campo `regra` com AlertCircle + destructive color
- [ ] Refatorar `DocumentationAccordion.tsx` — campo `prazo` com Clock ícone e side-by-side layout
- [ ] Refatorar `DocumentationAccordion.tsx` — steps como `<ol>` numerada
- [ ] Teste: `toAIContext(process)` retorna objeto plano válido
- [ ] Teste: Steps como array (não nested)
- [ ] Teste: Rules field populado corretamente
- [ ] Teste: formatting execution time (45 min, 2h 30m)
- [ ] Teste: null fields não incluídos em objeto (ou incluídos como null — verificar)
- [ ] Teste: DocumentationAccordion regra tem background destaque
- [ ] Rodar `npm run build` e `npm run typecheck`

---

## Observações

`toAIContext()` é um **transformer puro** — sem side effects, sem Server Actions, sem fetches. Apenas transforma dados já carregados.

Polymorphism via discriminated union (`'average_execution_time' in entity`):
- Se tem `average_execution_time` → Activity
- Se tem `activities_count` → Routine
- Se tem `systems_count` → Process

Steps em array separado (não nested em documentation) facilita **tokenização no Chatbot AI** — pode splitter por step para calcular usage tokens.

Campo `rules` (from `documentation.regra`) é **marcado para ênfase** — Chatbot AI pode usar isso para destacar regras críticas em respostas.

