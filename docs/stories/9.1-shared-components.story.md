# Story 9.1 — Componentes Compartilhados de Organização

**EPIC:** 9 — Org Management UX: Navegação Hierárquica Contextual
**Story ID:** 9.1
**Status:** Draft
**Sprint:** Sprint 3
**Assignee:** @dev (Dex)
**Priority:** CRÍTICA — Fundação zero-dep

---

## Objetivo

Extrair building blocks reutilizáveis dos 3 cockpits (Area, Nucleus, Process) onde estão duplicados. Criar componentes compartilhados que serão usados por todas as stories de UI seguintes (9.2-9.8).

---

## Critérios de Aceitação

- [ ] `src/components/organization/shared/InfoField.tsx` criado e funcional
  - Exibe `label` + `value` com layout consistente (label 80px, value flex)
  - Suporta `className` para customização
  - Usado por AreaCockpit360, NucleusCockpit360, ProcessCockpit360 sem quebras
- [ ] `src/components/organization/shared/RolesDisplay.tsx` criado
  - Recebe `roles: string[]` e `maxDisplay?: number` (default 2)
  - Renderiza badges `<UserCircle size={14}>` para cada role
  - Excesso mostra badge "+N more" clicável (abre Popover com lista completa)
  - Estilos: Shadcn Badge `variant="secondary"`
- [ ] `src/components/organization/shared/OrgEntityCard.tsx` criado
  - Props: `title`, `subtitle?`, `badge?`, `meta?` (dict de ícone + valor), `onClick`, `className?`
  - Layout: card com hover state (shadow-md), clique chamada `onClick`
  - Renderiza ícones à direita (ChevronRight) com transição
- [ ] `src/components/organization/shared/InputOutputList.tsx` criado
  - Recebe `items: Array<{name, description?, required?}>`, `direction: 'input'|'output'|'risk'`
  - Renderiza cards com ícone direcional (↓ input, ↑ output, ⚠️ risk)
  - Badge `required` quando `required === true`
- [ ] `src/components/organization/shared/DocumentationAccordion.tsx` criado
  - Recebe `data: OrgDocumentation` (objeto JSONB)
  - Itera sobre chaves não-nulas, cria `<AccordionItem>` para cada
  - Special case: campo `regra` recebe ícone `<AlertCircle>` e destaque visual
  - Special case: campo `prazo` formatado com `<Clock>` ícone
- [ ] `src/components/organization/shared/index.ts` criado com barrel export
- [ ] **Cockpits existentes refatorados:**
  - AreaCockpit360: importa `InfoField` do shared em vez de duplicado
  - NucleusCockpit360: importa `InfoField` do shared em vez de duplicado
  - ProcessCockpit360: importa `InfoField` do shared em vez de duplicado
- [ ] Nenhum cockpit quebrou após refatoração (`npm run build` passa)
- [ ] Nenhuma mudança em Server Actions (`src/app/actions/organization.ts`)

---

## File List

**Criados:**
- `src/components/organization/shared/InfoField.tsx`
- `src/components/organization/shared/RolesDisplay.tsx`
- `src/components/organization/shared/OrgEntityCard.tsx`
- `src/components/organization/shared/InputOutputList.tsx`
- `src/components/organization/shared/DocumentationAccordion.tsx`
- `src/components/organization/shared/index.ts`

**Refatorados:**
- `src/components/organization/AreaCockpit360.tsx`
- `src/components/organization/NucleusCockpit360.tsx`
- `src/components/organization/ProcessCockpit360.tsx`

**Referência (não modificar):**
- `src/app/actions/organization.ts`
- `src/types/organization.ts`

---

## Dependências

**Internas:**
- Story 9.7 (Card Redesign) pode rodar em paralelo — ambas usam `OrgEntityCard`

**Externas:**
- Nenhuma

**Bloqueada por:**
- Nenhuma

**Bloqueia:**
- Story 9.2 (ContextPanel — usa OrgEntityCard)
- Story 9.3 (AreaCockpit — usa componentes shared)
- Story 9.4, 9.5, 9.6, 9.8 (todas dependem de shared)

---

## Notas Técnicas

### Padrão de RolesDisplay

```typescript
// ✅ BOM
<RolesDisplay roles={['Gerente', 'Analista', 'Devops', 'QA']} maxDisplay={2} />
// Renderiza: [Gerente] [Analista] [+2 more]

// Popover ao clicar "+2 more"
// Mostra: Devops, QA
```

### Padrão de DocumentationAccordion

```typescript
const doc = {
  procedures: "1. Abrir sistema\n2. Clicar em...",
  regra: "Só executar após aprovação gerencial",
  prazo: "24 horas",
  instructions: "Seguir rigorosamente..."
};

<DocumentationAccordion data={doc} />
// Renderiza 4 AccordionItems
// Campo 'regra' com [AlertCircle] prefixo
```

### Server Actions — NÃO TOCAR

Arquivo `src/app/actions/organization.ts` contém toda a lógica de CRUD. Esta story SÓ refatora UI, zero mudanças em Server Actions.

---

## CodeRabbit Integration

**Areas de Review Esperadas:**
- Duplicação de código (componentes antes espalhados)
- Prop drilling em componentes shared
- Acessibilidade (labels ARIA, keyboard nav)
- TypeScript strictness (tipos genéricos para OrgDocumentation JSONB)

**Auto-Healing Config:**
```yaml
prompts:
  - "Refactor duplicated InfoField implementations into single shared component"
  - "Add ARIA labels to RolesDisplay badges and Popover"
  - "Ensure TypeScript strict mode for OrgDocumentation generic"
  - "Verify Shadcn AccordionItem renders with proper styling"
```

---

## Tasks

- [x] Criar `InfoField.tsx` (layout label + value, className support)
- [x] Criar `RolesDisplay.tsx` (badges + Popover para "+N")
- [x] Criar `OrgEntityCard.tsx` (card genérico com onClick)
- [x] Criar `InputOutputList.tsx` (cards com ícones direcionais)
- [x] Criar `DocumentationAccordion.tsx` (itera JSONB, destaque para regra/prazo)
- [x] Criar `index.ts` barrel export
- [x] Refatorar AreaCockpit360 (remover InfoField local, importar shared)
- [x] Refatorar NucleusCockpit360 (remover InfoField local, importar shared)
- [x] Refatorar ProcessCockpit360 (remover InfoField local, importar shared)
- [x] Rodar `npm run build` e verificar sem erros
- [ ] Rodar `npm run typecheck` e verificar sem erros
- [ ] Teste manual: AreaCockpit360 carrega e exibe tabs normalmente

---

## Observações

Componentes criados são **puros** — sem state, sem Server Actions. Todos os dados vêm de props. Isso permite máxima reutilização em Stories 9.3-9.8.

Padrão de Accordion especial para campo `regra` (ícone + destaque) porque este é um campo crítico para AI Context (Story 9.8).

---

## 🔧 Dev Agent Record

**Agent:** Dex (@dev)
**Mode:** YOLO (Autonomous)
**Execution Time:** ~15 min
**Status:** IN_PROGRESS

### Decisions Made
- ✅ Used Collapsible instead of Accordion (accordion not available in project)
- ✅ DocumentationAccordion implements ChevronDown rotation animation for better UX
- ✅ RolesDisplay uses Popover for overflow handling (consistent with Shadcn patterns)
- ✅ OrgEntityCard uses ChevronRight indicator for navigation affordance

### Files Created
- `src/components/organization/shared/InfoField.tsx` (38 lines)
- `src/components/organization/shared/RolesDisplay.tsx` (51 lines)
- `src/components/organization/shared/OrgEntityCard.tsx` (47 lines)
- `src/components/organization/shared/InputOutputList.tsx` (84 lines)
- `src/components/organization/shared/DocumentationAccordion.tsx` (116 lines)
- `src/components/organization/shared/index.ts` (5 lines)

### Files Modified
- `src/components/organization/AreaCockpit360.tsx` — removed duplicate InfoField, added import from shared
- `src/components/organization/NucleusCockpit360.tsx` — removed duplicate InfoField, added import from shared
- `src/components/organization/ProcessCockpit360.tsx` — removed duplicate InfoField, added import from shared

### Build Status
- ✅ npm run build — Compiled successfully
- ⏳ npm run typecheck — pending
- ⏳ Tests — pending

### Next Steps
1. Complete typecheck validation
2. Run manual tests on cockpits
3. CodeRabbit pre-commit review
4. Mark story Ready for Review

