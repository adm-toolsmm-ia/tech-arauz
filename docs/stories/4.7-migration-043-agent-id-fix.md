# Story 4.7 — Migration 043: Correção agent_id para agents(id)

Story ID: 4.7
Epic: Épico 4 — AI Features — SQL QA Chat + Gestão 360° de Agentes
Executor: @data-engineer (Dara)
Quality Gate: @dev (Dex)
Status: Done

## Objetivo

Criar migration 043 que altera as FKs de `agent_id` para referenciar `agents(id)` nas tabelas onde a semântica é "por agente" (definição), não "por run" (execução).

## Critérios de Aceite

- [x] Migration `043_fix_agent_id_references.sql` criada
- [x] DROP FK antigas; ADD FK novas para `agents(id)` com ON DELETE CASCADE/SET NULL conforme regra
- [x] Migração de dados: agent_sessions, agent_budgets, agent_usage_daily, agent_deployments, agent_tool_bindings, agent_context_bindings
- [x] `supabase/docs/SCHEMA.md` e `docs/architecture/data/schema.prisma` atualizados

## Tabelas Afetadas

- agent_sessions, agent_budgets, agent_usage_daily, agent_deployments, agent_tool_bindings, agent_context_bindings

## Change Log

- 2026-03-01: Done — Migration 043 implementada, schema e docs atualizados
