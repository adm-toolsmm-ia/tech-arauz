# Story 19.6: Espaider v2 Frontend-Visible Logs and Observability

**EPIC:** 19 - Espaider v2 Sync Migration
**Story ID:** 19.6
**Status:** READY
**Assignee:** @dev
**Priority:** HIGH
**Effort:** 2-3 days
**Depends on:** 19.3, 19.4, 19.5

---

## Goal

Ensure that the v2 sync emits operational logs that remain visible in the existing frontend integration history surfaces.

---

## Context for Implementation

The current product already exposes log history in the integrations frontend via:

- `integration_log_entries`
- `sync_logs`
- `/api/integracoes/logs`
- `/api/integracoes/logs/summary`

The v2 implementation must preserve or improve that visibility.

Operational requirement from the epic:

- log connection attempt
- sync start
- page fetches when relevant
- dataset success/failure
- quarantine counts
- sync end summary

---

## Acceptance Criteria

- [ ] v2 sync writes frontend-visible logs using existing or explicitly evolved logging tables
- [ ] Each sync run has a correlation key or request_id
- [ ] Key events are logged:
  - connection attempt
  - sync started
  - page fetched
  - dataset processed
  - quarantine summary
  - sync completed
  - sync failed
- [ ] Log messages are actionable and sanitized
- [ ] Existing frontend log viewer can display v2 events without breaking
- [ ] Summary endpoints continue to return meaningful status for v2 runs
- [ ] Tests cover log persistence and API output for the viewer

---

## Architecture Notes

Preferred approach:

- keep the current frontend-visible log path compatible
- add structure through `details` fields instead of breaking the viewer contract
- avoid inventing a second invisible logging system for v2

If schema evolution is needed for logs:

- make it additive
- preserve backward compatibility for existing UI queries

---

## File List

**Create:**
- `src/lib/sync/logger-v2.ts` if a dedicated v2 logger wrapper is needed
- `src/app/api/integracoes/logs/__tests__/v2-logs.test.ts`

**Modify:**
- `src/lib/sync/espaider-v2-sync.ts`
- `src/app/api/integracoes/logs/route.ts`
- `src/app/api/integracoes/logs/summary/route.ts`
- `src/components/integracoes/LogViewer.tsx` only if additive display support is needed

---

## Testing Checklist

- [ ] v2 sync writes visible log entries
- [ ] summary route reflects v2 runs correctly
- [ ] failed run logs include sanitized reason
- [ ] quarantine count appears in details or summary
- [ ] viewer API remains backward compatible

**Run:**
```bash
npm run lint
npm run typecheck
npm run test -- src/app/api/integracoes/logs/__tests__/v2-logs.test.ts
```

---

## Definition of Done

- [ ] v2 logs visible in frontend path
- [ ] operational event coverage complete
- [ ] backward compatibility preserved
- [ ] tests passing
- [ ] checklist and file list updated

