# 2026-02-27 — Story 2.8: Baseline de Acessibilidade

## Resumo

Implementado o baseline de acessibilidade (WCAG 2.1 AA) no portal Tech Arauz.

## O que foi feito

### Tooling instalado
- `eslint-plugin-jsx-a11y` como devDependency
- `@axe-core/react` como devDependency

### Configuração ESLint (`.eslintrc.json`)
- Plugin `jsx-a11y` adicionado com todas as regras críticas em `"error"`
- `media-has-caption` e `no-autofocus` configurados como `"warn"` (aceitável)

### AxeProvider (`src/components/providers/AxeProvider.tsx`)
- Componente criado com dynamic import condicional (dev only)
- Integrado no `src/components/providers.tsx`

### Correções jsx-a11y (13 arquivos)
- `CronogramaCalendar.tsx` — div popover com onClick → role+tabIndex+onKeyDown
- `CronogramaCockpit.tsx` — div atividade com onClick → role+tabIndex+onKeyDown
- `dashboard-content.tsx` — div projetos recentes com onClick → role+tabIndex+onKeyDown
- `login/page.tsx` — `<a href="#">` → `<button type="button">`
- `AgentTypeCard.tsx` — div ações stopPropagation → role=presentation
- `AgentCardStandard.tsx` — div ações stopPropagation → role=presentation
- `LogViewer.tsx` — div log entry condicional → role/tabIndex/onKeyDown dinâmicos
- `ModelCard.tsx` — div ações stopPropagation → role=presentation
- `ModelsListView.tsx` — tr clicável → role+tabIndex+onKeyDown; div checkbox mobile → role=presentation
- `ProviderCard.tsx (lm-models)` — div ações → role=presentation
- `ProviderCard.tsx (lm-providers)` — div ações → role=presentation
- `ProjectListView.tsx` — div mobile card com onClick → role+tabIndex+onKeyDown
- `card.tsx (CardTitle)` — `<h3>` via forwardRef com children explícito (fix heading-has-content)

### Artefatos criados
- `npm run a11y:check` adicionado ao `package.json` → `next lint --max-warnings=0`
- `.aios-core/development/checklists/a11y-checklist.md`

### Resultado do lint
```
0 errors | 1 warning (autoFocus no FilterControl — configurado como warn)
```

## Backlog identificado (Story 2.9)
- `aria-label` em ícones icon-only (Trash2, Edit sem texto visível)
- Verificação de contraste AA (light + dark mode)
- Screen reader test (NVDA/VoiceOver) no fluxo de login
