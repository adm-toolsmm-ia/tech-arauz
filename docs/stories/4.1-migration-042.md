# Story 4.1 — Database Foundation: Migration 042

Story ID: 4.1
Epic: Épico 4 — AI Features — SQL QA Chat + Gestão 360° de Agentes
Sprint: 6 — AI Infrastructure
Agente: @data-engineer (Dara)
QA Gate: @qa (Quinn)
Esforço: 2 dias
Prioridade: CRÍTICA (bloqueante)
Status: Draft

---

## Objetivo

Criar migration 042 com 11 tabelas de suporte para chat conversacional com agentes LLM, observabilidade operacional, controle de budget, e rastreamento de deployments. Esta migration é **bloqueante** — todas as 6 stories seguintes dependem dela.

---

## Como Usuário

Como administrador de sistema,
quero que todas as tabelas necessárias para o sistema de chat AI, observabilidade e gestão de agentes estejam no banco de dados,
para que os desenvolvedores possam implementar endpoints REST, LCEL chains, frontend UI e dashboard 360° com segurança, observabilidade e isolamento multi-tenant.

---

## Contexto

**Situação atual:**
- Migration 041 criou tabelas base para agentes (agent_runs, agent_configs, etc.)
- Migrations 028 e 040 estabeleceram padrões RLS e auditoria

**Mudança necessária:**
- 11 novas tabelas para expandir o sistema de agentes:
  - Chat: `agent_sessions`, `agent_messages`, `agent_run_steps`
  - Observabilidade: `agent_budgets`, `agent_usage_daily`, `agent_deployments`
  - Suporte: `tools`, `agent_tool_bindings`, `context_providers`, `agent_context_bindings`, `agent_feedback`, `lm_provider_accounts`
- ALTERs nas tabelas existentes (`agent_runs`, `lm_models`) para expandir capacidade
- RLS policies seguindo ADR-001: USING (true) WITH CHECK (true) em todas
- Índices estratégicos para performance queries de agregação (período, tenant, serviço)
- Archival info para retenção de logs

**Padrão de referência:**
- Migration 028 (`supabase/migrations/028_create_agents_schema.sql`) — estrutura RLS base
- Migration 040 (`supabase/migrations/040_expand_rls_audit_tables.sql`) — função `audit_all_rls_policies()`

---

## Critérios de Aceite

### Tabelas Criadas

- [ ] **agent_sessions**
  - Campos: id (UUID PK), tenant_id (UUID, NOT NULL), user_id (UUID), agent_id (UUID), started_at (timestamp), ended_at (timestamp), status (text: "active"/"paused"/"closed"), token_usage (integer), created_at, updated_at
  - FK: user_id → auth.users(id), agent_id → agent_runs(id)
  - Índices: (tenant_id, agent_id, created_at DESC), (tenant_id, status)
  - RLS: true WITH CHECK (true)

- [ ] **agent_messages**
  - Campos: id (UUID PK), session_id (UUID, NOT NULL), tenant_id (UUID, NOT NULL), role (text: "user"/"assistant"), content (text), metadata (jsonb), tokens_used (integer), created_at
  - FK: session_id → agent_sessions(id)
  - Índices: (session_id, created_at ASC), (tenant_id, created_at DESC)
  - RLS: true WITH CHECK (true)

- [ ] **agent_run_steps**
  - Campos: id (UUID PK), agent_id (UUID, NOT NULL), tenant_id (UUID, NOT NULL), run_id (UUID, NOT NULL), step_type (text: "input"/"tool_call"/"observation"/"output"), input (jsonb), output (jsonb), metadata (jsonb), execution_time_ms (integer), created_at
  - FK: run_id → agent_runs(id)
  - Índices: (agent_id, run_id), (tenant_id, created_at DESC)
  - RLS: true WITH CHECK (true)

- [ ] **agent_budgets**
  - Campos: id (UUID PK), tenant_id (UUID, NOT NULL), agent_id (UUID, NOT NULL), monthly_limit_usd (numeric), current_month_spent_usd (numeric), reset_date (date), created_at, updated_at
  - Índices: (tenant_id, agent_id), (reset_date)
  - RLS: true WITH CHECK (true)

- [ ] **agent_usage_daily**
  - Campos: id (UUID PK), tenant_id (UUID, NOT NULL), agent_id (UUID, NOT NULL), date (date), sessions_count (integer), messages_count (integer), cost_total_usd (numeric), avg_latency_ms (numeric), success_rate_pct (numeric), created_at
  - Índices: (tenant_id, agent_id, date DESC), (date DESC)
  - RLS: true WITH CHECK (true)

- [ ] **agent_deployments**
  - Campos: id (UUID PK), agent_id (UUID, NOT NULL), tenant_id (UUID, NOT NULL), version (text), environment (text: "dev"/"staging"/"prod"), deployed_at (timestamp), deployed_by (UUID), config_hash (text), status (text: "success"/"failed"/"rolled_back"), created_at
  - FK: deployed_by → auth.users(id)
  - Índices: (agent_id, deployed_at DESC), (tenant_id, environment, status)
  - RLS: true WITH CHECK (true)

- [ ] **tools**
  - Campos: id (UUID PK), tenant_id (UUID, NOT NULL), tool_name (text), description (text), config_schema (jsonb), enabled (boolean DEFAULT true), created_at, updated_at
  - Índices: (tenant_id, tool_name), (enabled)
  - RLS: true WITH CHECK (true)

- [ ] **agent_tool_bindings**
  - Campos: id (UUID PK), agent_id (UUID, NOT NULL), tool_id (UUID, NOT NULL), tenant_id (UUID, NOT NULL), enabled (boolean DEFAULT true), config_overrides (jsonb), created_at, updated_at
  - FK: agent_id → agent_runs(id), tool_id → tools(id)
  - Índices: (agent_id, enabled), (tenant_id, agent_id)
  - RLS: true WITH CHECK (true)

- [ ] **context_providers**
  - Campos: id (UUID PK), tenant_id (UUID, NOT NULL), provider_type (text: "sql"/"api"/"knowledge_base"), description (text), config (jsonb), enabled (boolean DEFAULT true), created_at, updated_at
  - Índices: (tenant_id, provider_type), (enabled)
  - RLS: true WITH CHECK (true)

- [ ] **agent_context_bindings**
  - Campos: id (UUID PK), agent_id (UUID, NOT NULL), context_id (UUID, NOT NULL), tenant_id (UUID, NOT NULL), priority (integer), created_at
  - FK: agent_id → agent_runs(id), context_id → context_providers(id)
  - Índices: (agent_id, priority), (tenant_id, agent_id)
  - RLS: true WITH CHECK (true)

- [ ] **agent_feedback**
  - Campos: id (UUID PK), session_id (UUID, NOT NULL), tenant_id (UUID, NOT NULL), rating (integer: 1-5), feedback_text (text), feedback_type (text: "quality"/"accuracy"/"relevance"/"performance"), created_at
  - FK: session_id → agent_sessions(id)
  - Índices: (session_id), (tenant_id, created_at DESC), (feedback_type)
  - RLS: true WITH CHECK (true)

- [ ] **lm_provider_accounts**
  - Campos: id (UUID PK), tenant_id (UUID, NOT NULL), provider (text: "openai"/"anthropic"/"google"/"azure"), api_key_encrypted (text), account_identifier (text), monthly_spend_usd (numeric), quota_limit_usd (numeric), created_at, updated_at
  - Índices: (tenant_id, provider)
  - RLS: true WITH CHECK (true)
  - **IMPORTANTE:** Criptografia de `api_key_encrypted` usando Supabase Vault (ou encrypted field type)

### ALTERs em Tabelas Existentes

- [ ] **agent_runs**
  - ADD COLUMN: budget_limit_usd (numeric)
  - ADD COLUMN: session_id (UUID FK → agent_sessions)
  - ADD CONSTRAINT: FK session_id is ON DELETE SET NULL

- [ ] **lm_models**
  - ADD COLUMN: input_token_cost_usd (numeric)
  - ADD COLUMN: output_token_cost_usd (numeric)
  - ADD COLUMN: provider_account_id (UUID FK → lm_provider_accounts)

### Policies e Segurança

- [ ] RLS policies aplicadas via `audit_all_rls_policies()` (Migration 040)
  - Todas policies: USING (true) WITH CHECK (true)
  - Nenhuma query expõe dados cross-tenant

- [ ] Índices criados com INCLUDE (tenant_id) para service filtering
  - Queries com `.eq('tenant_id', X)` executam < 50ms

### Auditorias e Validações

- [ ] `npm run audit:rls` passa 100% (todas policies testadas)
- [ ] Testes de cross-tenant bloqueados:
  - User A não vê dados de User B quando tenant_id é diferente
  - Admin queries com `select *` retornam apenas tenant atual
- [ ] Rollback procedure testada:
  - Migration pode ser revertida sem data loss

---

## Implementação Necessária

### 1. Criar arquivo `supabase/migrations/20260301_042_ai_features_chat_and_360.sql`

**Estrutura do arquivo:**

```sql
-- Migration 042: AI Features Chat & 360° Dashboard
-- Date: 2026-03-01
-- Purpose: Create 11 tables for chat sessions, observability, budgets, tools, context, feedback, LLM accounts
-- Dependencies: Migration 041 (agent_runs, agent_configs, lm_models)

-- Enable required extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- ============================================
-- 1. CHAT TABLES
-- ============================================

CREATE TABLE agent_sessions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  tenant_id UUID NOT NULL,
  user_id UUID,
  agent_id UUID,
  started_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  ended_at TIMESTAMP WITH TIME ZONE,
  status TEXT DEFAULT 'active' CHECK (status IN ('active', 'paused', 'closed')),
  token_usage INTEGER DEFAULT 0,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_sessions_user FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE SET NULL,
  CONSTRAINT fk_agent_sessions_agent FOREIGN KEY (agent_id) REFERENCES agent_runs(id) ON DELETE SET NULL,
  CONSTRAINT fk_agent_sessions_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_agent_sessions_tenant_agent_created
  ON agent_sessions(tenant_id, agent_id, created_at DESC);
CREATE INDEX idx_agent_sessions_tenant_status
  ON agent_sessions(tenant_id, status);

-- RLS Policy
ALTER TABLE agent_sessions ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_sessions_tenant_isolation ON agent_sessions
  USING (TRUE) WITH CHECK (TRUE);
COMMENT ON POLICY agent_sessions_tenant_isolation ON agent_sessions
  IS 'ADR-001: All tenant isolation enforced at application layer; RLS allows all for flexibility';

CREATE TABLE agent_messages (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  session_id UUID NOT NULL,
  tenant_id UUID NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('user', 'assistant')),
  content TEXT NOT NULL,
  metadata JSONB DEFAULT '{}'::JSONB,
  tokens_used INTEGER DEFAULT 0,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_messages_session FOREIGN KEY (session_id) REFERENCES agent_sessions(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_messages_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_agent_messages_session_created
  ON agent_messages(session_id, created_at ASC);
CREATE INDEX idx_agent_messages_tenant_created
  ON agent_messages(tenant_id, created_at DESC);

ALTER TABLE agent_messages ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_messages_tenant_isolation ON agent_messages
  USING (TRUE) WITH CHECK (TRUE);

CREATE TABLE agent_run_steps (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  agent_id UUID NOT NULL,
  tenant_id UUID NOT NULL,
  run_id UUID NOT NULL,
  step_type TEXT NOT NULL CHECK (step_type IN ('input', 'tool_call', 'observation', 'output')),
  input JSONB DEFAULT '{}'::JSONB,
  output JSONB DEFAULT '{}'::JSONB,
  metadata JSONB DEFAULT '{}'::JSONB,
  execution_time_ms INTEGER,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_run_steps_run FOREIGN KEY (run_id) REFERENCES agent_runs(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_run_steps_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_agent_run_steps_agent_run
  ON agent_run_steps(agent_id, run_id);
CREATE INDEX idx_agent_run_steps_tenant_created
  ON agent_run_steps(tenant_id, created_at DESC);

ALTER TABLE agent_run_steps ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_run_steps_tenant_isolation ON agent_run_steps
  USING (TRUE) WITH CHECK (TRUE);

-- ============================================
-- 2. OBSERVABILITY TABLES
-- ============================================

CREATE TABLE agent_budgets (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  tenant_id UUID NOT NULL,
  agent_id UUID NOT NULL,
  monthly_limit_usd NUMERIC(10, 2),
  current_month_spent_usd NUMERIC(10, 2) DEFAULT 0,
  reset_date DATE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_budgets_agent FOREIGN KEY (agent_id) REFERENCES agent_runs(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_budgets_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_agent_budgets_tenant_agent
  ON agent_budgets(tenant_id, agent_id);
CREATE INDEX idx_agent_budgets_reset_date
  ON agent_budgets(reset_date);

ALTER TABLE agent_budgets ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_budgets_tenant_isolation ON agent_budgets
  USING (TRUE) WITH CHECK (TRUE);

CREATE TABLE agent_usage_daily (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  tenant_id UUID NOT NULL,
  agent_id UUID NOT NULL,
  date DATE NOT NULL,
  sessions_count INTEGER DEFAULT 0,
  messages_count INTEGER DEFAULT 0,
  cost_total_usd NUMERIC(10, 2) DEFAULT 0,
  avg_latency_ms NUMERIC(10, 2) DEFAULT 0,
  success_rate_pct NUMERIC(5, 2) DEFAULT 100,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_usage_daily_agent FOREIGN KEY (agent_id) REFERENCES agent_runs(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_usage_daily_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE,
  UNIQUE(tenant_id, agent_id, date)
);

CREATE INDEX idx_agent_usage_daily_tenant_agent_date
  ON agent_usage_daily(tenant_id, agent_id, date DESC);
CREATE INDEX idx_agent_usage_daily_date
  ON agent_usage_daily(date DESC);

ALTER TABLE agent_usage_daily ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_usage_daily_tenant_isolation ON agent_usage_daily
  USING (TRUE) WITH CHECK (TRUE);

CREATE TABLE agent_deployments (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  agent_id UUID NOT NULL,
  tenant_id UUID NOT NULL,
  version TEXT NOT NULL,
  environment TEXT NOT NULL CHECK (environment IN ('dev', 'staging', 'prod')),
  deployed_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  deployed_by UUID,
  config_hash TEXT,
  status TEXT DEFAULT 'success' CHECK (status IN ('success', 'failed', 'rolled_back')),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_deployments_agent FOREIGN KEY (agent_id) REFERENCES agent_runs(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_deployments_deployed_by FOREIGN KEY (deployed_by) REFERENCES auth.users(id) ON DELETE SET NULL,
  CONSTRAINT fk_agent_deployments_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_agent_deployments_agent_deployed_at
  ON agent_deployments(agent_id, deployed_at DESC);
CREATE INDEX idx_agent_deployments_tenant_env_status
  ON agent_deployments(tenant_id, environment, status);

ALTER TABLE agent_deployments ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_deployments_tenant_isolation ON agent_deployments
  USING (TRUE) WITH CHECK (TRUE);

-- ============================================
-- 3. TOOLS & CONTEXT TABLES
-- ============================================

CREATE TABLE tools (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  tenant_id UUID NOT NULL,
  tool_name TEXT NOT NULL,
  description TEXT,
  config_schema JSONB DEFAULT '{}'::JSONB,
  enabled BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_tools_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_tools_tenant_name
  ON tools(tenant_id, tool_name);
CREATE INDEX idx_tools_enabled
  ON tools(enabled);

ALTER TABLE tools ENABLE ROW LEVEL SECURITY;
CREATE POLICY tools_tenant_isolation ON tools
  USING (TRUE) WITH CHECK (TRUE);

CREATE TABLE agent_tool_bindings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  agent_id UUID NOT NULL,
  tool_id UUID NOT NULL,
  tenant_id UUID NOT NULL,
  enabled BOOLEAN DEFAULT TRUE,
  config_overrides JSONB DEFAULT '{}'::JSONB,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_tool_bindings_agent FOREIGN KEY (agent_id) REFERENCES agent_runs(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_tool_bindings_tool FOREIGN KEY (tool_id) REFERENCES tools(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_tool_bindings_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_agent_tool_bindings_agent_enabled
  ON agent_tool_bindings(agent_id, enabled);
CREATE INDEX idx_agent_tool_bindings_tenant_agent
  ON agent_tool_bindings(tenant_id, agent_id);

ALTER TABLE agent_tool_bindings ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_tool_bindings_tenant_isolation ON agent_tool_bindings
  USING (TRUE) WITH CHECK (TRUE);

CREATE TABLE context_providers (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  tenant_id UUID NOT NULL,
  provider_type TEXT NOT NULL CHECK (provider_type IN ('sql', 'api', 'knowledge_base')),
  description TEXT,
  config JSONB DEFAULT '{}'::JSONB,
  enabled BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_context_providers_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_context_providers_tenant_type
  ON context_providers(tenant_id, provider_type);
CREATE INDEX idx_context_providers_enabled
  ON context_providers(enabled);

ALTER TABLE context_providers ENABLE ROW LEVEL SECURITY;
CREATE POLICY context_providers_tenant_isolation ON context_providers
  USING (TRUE) WITH CHECK (TRUE);

CREATE TABLE agent_context_bindings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  agent_id UUID NOT NULL,
  context_id UUID NOT NULL,
  tenant_id UUID NOT NULL,
  priority INTEGER DEFAULT 0,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_context_bindings_agent FOREIGN KEY (agent_id) REFERENCES agent_runs(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_context_bindings_context FOREIGN KEY (context_id) REFERENCES context_providers(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_context_bindings_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_agent_context_bindings_agent_priority
  ON agent_context_bindings(agent_id, priority);
CREATE INDEX idx_agent_context_bindings_tenant_agent
  ON agent_context_bindings(tenant_id, agent_id);

ALTER TABLE agent_context_bindings ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_context_bindings_tenant_isolation ON agent_context_bindings
  USING (TRUE) WITH CHECK (TRUE);

-- ============================================
-- 4. FEEDBACK & LM ACCOUNTS TABLES
-- ============================================

CREATE TABLE agent_feedback (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  session_id UUID NOT NULL,
  tenant_id UUID NOT NULL,
  rating INTEGER CHECK (rating >= 1 AND rating <= 5),
  feedback_text TEXT,
  feedback_type TEXT CHECK (feedback_type IN ('quality', 'accuracy', 'relevance', 'performance')),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_agent_feedback_session FOREIGN KEY (session_id) REFERENCES agent_sessions(id) ON DELETE CASCADE,
  CONSTRAINT fk_agent_feedback_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_agent_feedback_session
  ON agent_feedback(session_id);
CREATE INDEX idx_agent_feedback_tenant_created
  ON agent_feedback(tenant_id, created_at DESC);
CREATE INDEX idx_agent_feedback_type
  ON agent_feedback(feedback_type);

ALTER TABLE agent_feedback ENABLE ROW LEVEL SECURITY;
CREATE POLICY agent_feedback_tenant_isolation ON agent_feedback
  USING (TRUE) WITH CHECK (TRUE);

CREATE TABLE lm_provider_accounts (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  tenant_id UUID NOT NULL,
  provider TEXT NOT NULL CHECK (provider IN ('openai', 'anthropic', 'google', 'azure')),
  api_key_encrypted TEXT NOT NULL,
  account_identifier TEXT,
  monthly_spend_usd NUMERIC(10, 2) DEFAULT 0,
  quota_limit_usd NUMERIC(10, 2),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  CONSTRAINT fk_lm_provider_accounts_tenant FOREIGN KEY (tenant_id) REFERENCES auth.tenants(id) ON DELETE CASCADE
);

CREATE INDEX idx_lm_provider_accounts_tenant_provider
  ON lm_provider_accounts(tenant_id, provider);

ALTER TABLE lm_provider_accounts ENABLE ROW LEVEL SECURITY;
CREATE POLICY lm_provider_accounts_tenant_isolation ON lm_provider_accounts
  USING (TRUE) WITH CHECK (TRUE);

-- ============================================
-- 5. ALTER EXISTING TABLES
-- ============================================

ALTER TABLE agent_runs
  ADD COLUMN IF NOT EXISTS budget_limit_usd NUMERIC(10, 2),
  ADD COLUMN IF NOT EXISTS session_id UUID REFERENCES agent_sessions(id) ON DELETE SET NULL;

ALTER TABLE lm_models
  ADD COLUMN IF NOT EXISTS input_token_cost_usd NUMERIC(12, 6),
  ADD COLUMN IF NOT EXISTS output_token_cost_usd NUMERIC(12, 6),
  ADD COLUMN IF NOT EXISTS provider_account_id UUID REFERENCES lm_provider_accounts(id) ON DELETE SET NULL;

-- ============================================
-- 6. AUDIT & COMMENTS
-- ============================================

COMMENT ON TABLE agent_sessions IS 'Chat session contexts per tenant, user, and agent';
COMMENT ON TABLE agent_messages IS 'Conversation turns (user messages and assistant responses)';
COMMENT ON TABLE agent_run_steps IS 'Detailed execution steps within agent runs (LCEL chain)';
COMMENT ON TABLE agent_budgets IS 'Monthly spend limits and current usage per agent/tenant';
COMMENT ON TABLE agent_usage_daily IS 'Daily aggregated metrics (sessions, messages, cost, latency, success rate)';
COMMENT ON TABLE agent_deployments IS 'Deployment history and versioning per agent';
COMMENT ON TABLE tools IS 'Tool definitions available to agents';
COMMENT ON TABLE agent_tool_bindings IS 'Which tools are enabled per agent with config overrides';
COMMENT ON TABLE context_providers IS 'Context sources (SQL, API, knowledge base) per tenant';
COMMENT ON TABLE agent_context_bindings IS 'Which context sources are used per agent, with priority';
COMMENT ON TABLE agent_feedback IS 'User feedback on chat quality (1-5 rating), type, and comments';
COMMENT ON TABLE lm_provider_accounts IS 'LLM provider credentials (OpenAI, Anthropic, Google, Azure) encrypted';

-- ============================================
-- 7. CALL AUDIT FUNCTION (Migration 040)
-- ============================================

SELECT audit_all_rls_policies();
```

**Notas importantes:**
- Usar `uuid_generate_v4()` para PKs (não UUID bigint ou serial)
- Usar `TIMESTAMP WITH TIME ZONE` (não DATE, quando tempo é relevante)
- Foreign keys com `ON DELETE CASCADE` ou `ON DELETE SET NULL` conforme contexto
- RLS policies: `USING (TRUE) WITH CHECK (TRUE)` seguindo ADR-001
- Índices incluem `tenant_id` para service filtering eficiente
- Comentários em cada tabela explicam seu propósito

### 2. Validar Migration

```bash
# Dry-run
supabase migration status

# Apply migration (em ambiente de development)
supabase db push

# Verify tables created
psql -d local_db -c "\dt agent_*"

# Check RLS policies
psql -d local_db -c "\dp agent_sessions"

# Check indexes
psql -d local_db -c "\di idx_agent_*"
```

### 3. Testes de Segurança

**Test 1: Cross-tenant isolation**
```sql
-- Simular User A (tenant_id = abc123)
SELECT * FROM agent_sessions WHERE tenant_id = 'abc123';

-- Simular User B (tenant_id = def456) — NÃO deve ver dados de abc123
SELECT * FROM agent_sessions WHERE tenant_id = 'def456';
```

**Test 2: RLS audit**
```bash
npm run audit:rls
```
Verificar que todas as 10 policies aparecem com status ✅

**Test 3: Index performance**
```sql
EXPLAIN ANALYZE
SELECT * FROM agent_messages
WHERE tenant_id = '...' AND created_at > NOW() - INTERVAL '7 days'
ORDER BY created_at DESC LIMIT 100;
-- Expected: Index Scan (não Seq Scan)
```

---

## File List

### Criados
- [ ] `supabase/migrations/20260301_042_ai_features_chat_and_360.sql` — Migration principal (11 tabelas + ALTERs)

### Modificados
- [ ] `supabase/migrations/040_expand_rls_audit_tables.sql` — Nenhum — referenciado apenas (função `audit_all_rls_policies()`)

### Auditoria Pós-Implementação
- [ ] Migration log: `supabase/migrations/.MIGRATION_LOG` (append success status)
- [ ] DB schema dump: `docs/architecture/SCHEMA.md` (update com tabelas 042)
- [ ] RLS audit report: `docs/architecture/RLS-AUDIT-042.md` (criado automaticamente por `npm run audit:rls`)

---

## Dependências

- Migração 041 (agent_runs, agent_configs, lm_models) — deve estar já aplicada
- Migração 040 (função `audit_all_rls_policies()`) — deve estar disponível no schema
- Supabase CLI 1.185+ (para migration apply)
- PostgreSQL 14+ (para uuid-ossp, pgcrypto extensions)

---

## Story Links

**Épico:** [Épico 4 — AI Features](./epic-4-ai-features-chat-360.md)

**Histórias bloqueadas por esta story:**
- [Story 4.2 — Backend Core: Multi-provider Factory + SQL Validator](./4.2-backend-factory-validator.md)
- [Story 4.3 — Backend Chat: Session Service + Endpoints](./4.3-backend-session-service.md)
- [Story 4.6 — Frontend: Dashboard 360°](./4.6-frontend-dashboard-360.md)

---

## Dev Notes

### Padrão RLS (ADR-001)

Todas as 10 tabelas seguem o padrão **USING (true) WITH CHECK (true)**:
- Isolamento de tenant é responsabilidade da **aplicação**, não do banco
- RLS ativa apenas como "escudo de última linha"
- Queries sempre incluem `.eq('tenant_id', X)` no code

[Fonte: .claude/CLAUDE.md → Constitution Article VI; ADR-001 in memory]

### Nomenclatura Índices

Padrão: `idx_{table}_{columns}`
- Exemplo: `idx_agent_messages_tenant_created` para (tenant_id, created_at DESC)
- Sempre incluir tenant_id como coluna 1 para service filtering

[Fonte: memory/project-state/known-gaps.md]

### Criptografia de api_key_encrypted

Supabase Vault oferece encrypted field type, mas requer extensão `pgcrypto`:
```sql
CREATE EXTENSION pgcrypto;
-- Field será armazenado como:
api_key_encrypted TEXT NOT NULL  -- encrypted by application or trigger
```

Alternativa: Aplicação criptografa antes de INSERT usando `crypto.encrypt(key)`

[Nota: Confirmar com @data-engineer se usando Vault ou application-layer encryption]

### Função audit_all_rls_policies()

Definida em Migration 040. Ao executar `SELECT audit_all_rls_policies()` no fim desta migration:
- Verifica que TODAS as tabelas têm RLS ENABLED
- Verifica que TODAS as policies estão com USING (true) WITH CHECK (true)
- Registra em log se alguma policy está faltando

Saída esperada:
```
✅ agent_sessions: RLS enabled, 1 policy found (agent_sessions_tenant_isolation)
✅ agent_messages: RLS enabled, 1 policy found (agent_messages_tenant_isolation)
...
```

---

## Checklist de Qualidade

- [ ] Schema validado contra padrão AIOS (UUIDs, timestamps, constraints)
- [ ] RLS testado com 3 tenants simulados (cross-tenant bloqueado)
- [ ] Índices explicados via EXPLAIN ANALYZE (<50ms queries)
- [ ] Foreign keys e constraints testados (integridade referencial)
- [ ] Rollback procedure testada (migration pode revert sem data loss)
- [ ] Migration roda sem erros: `supabase db push`
- [ ] npm run audit:rls passa 100% (10 policies validadas)
- [ ] Nenhum hardcoded value (usar $1, $2 em queries)
- [ ] Tabelas documentadas via COMMENT ON TABLE

---

**Story criada:** 2026-03-01
**Versão:** 1.0.0
**Próximo passo:** @po valida story com `*validate-story-draft 4.1`
