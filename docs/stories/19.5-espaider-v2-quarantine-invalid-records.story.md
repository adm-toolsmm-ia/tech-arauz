# Story 19.5: Espaider v2 Invalid Record Quarantine

**EPIC:** 19 - Espaider v2 Sync Migration
**Story ID:** 19.5
**Status:** Done
**Assignee:** @data-engineer
**Priority:** HIGH
**Effort:** 1-2 days
**Depends on:** 19.2, 19.3, 19.4

---

## Goal

Introduce a quarantine mechanism for invalid v2 records so that the sync remains reliable and every rejected item has a traceable reason.

---

## Context for Implementation

EPIC 19 explicitly requires isolation of invalid items with traceable reasons.

The current logs alone are not enough for reliable reprocessing and audit if a large batch contains mixed valid and invalid records.

This story should prefer a dedicated persistence mechanism over ephemeral console logs.

---

## Acceptance Criteria

- [x] Invalid parent or child records are isolated instead of silently dropped
- [x] Quarantine persistence stores:
  - tenant_id
  - request_id or correlation_id
  - dataset
  - stage
  - raw payload
  - validation or processing reason
  - timestamps
- [x] Sync summary includes quarantine counts
- [x] Frontend-visible logs mention quarantined counts without leaking secrets
- [x] Reprocessing strategy from quarantine is documented (table-based, manual reprocessing via SQL or future tooling)
- [x] Tests cover schema-invalid and processing-invalid scenarios

---

## Architecture Notes

Recommended implementation:

- add a dedicated quarantine table
- write quarantine entries during validation or mapping failure
- do not fail the entire sync run for item-level data defects unless threshold policy requires it

Recommended table name:

- `integration_sync_quarantine`

If another name is chosen, document the reason and keep it integration-scoped rather than project-domain-specific.

---

## File List

**Create:**
- `supabase/migrations/*_create_integration_sync_quarantine.sql`
- `src/lib/sync/quarantine.ts`
- `src/lib/sync/__tests__/quarantine.test.ts`

**Modify:**
- `src/lib/sync/espaider-v2-sync.ts`
- `src/app/api/integracoes/logs/route.ts` only if extra filtering or visibility is needed

---

## Testing Checklist

- [x] stores invalid parent record with reason
- [x] stores invalid child record with dataset identifier
- [x] sync run completes with mixed valid/invalid records
- [x] quarantine count is reflected in summary/log output
- [x] no secrets are persisted in log-facing message fields

**Run:**
```bash
npm run lint
npm run typecheck
npm run test -- src/lib/sync/__tests__/quarantine.test.ts
```

---

## Definition of Done

- [x] quarantine persistence implemented
- [x] invalid items isolated with traceable reason
- [x] summary/log integration present
- [x] tests passing (13 tests)
- [x] checklist and file list updated

