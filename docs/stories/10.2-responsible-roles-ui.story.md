# Story 10.2 — Redesign Responsible Roles UI Component (Tags + Autocomplete)

**EPIC:** 10 — Organization Knowledge Graph Refinement & Synchronization
**Story ID:** 10.2
**Status:** ✅ **DONE** (v0.2.3+)
**Sprint:** Sprint 3
**Assignee:** @ux-design-expert (Uma) + @dev (Dex)
**Priority:** HIGH
**Deployed:** 2026-03-XX (commit c50c698)

---

## Objetivo

Criar componente reusável `ResponsibleRolesInput` com tags + autocomplete para entrada de responsible roles. Implementar role registry em `src/lib/organization/role-definitions.ts` com enums e descrições. Integrar em `OrgEntityFormSheet` para suppliers, services, documents. Implementar navegação de teclado, acessibilidade WCAG AA, e responsividade.

---

## Critérios de Aceitação

- [ ] `src/lib/organization/role-definitions.ts` criado
  - [ ] `ORGANIZATION_ROLES` constant com role categories (management, specialist, operational, external)
  - [ ] Interface `RoleDefinition` com `value`, `label`, `description`, `category`
  - [ ] `getRolesByCategory(category)` função helper
  - [ ] `getAllRoles()` função helper
  - [ ] Documentação de padrão extensível para EPIC 11 (DB-driven roles)
- [ ] `ResponsibleRolesInput` component criado (`src/components/organization/ResponsibleRolesInput.tsx`)
  - [ ] Props: `value: string[]`, `onChange: (roles: string[]) => void`, `disabled?: boolean`
  - [ ] Renderiza tags com badge styling (shadcn/ui Badge)
  - [ ] Remove tag com X button (click or keyboard)
  - [ ] Command/Autocomplete para adicionar roles (usando shadcn/ui Command)
  - [ ] Keyboard nav:
    - [ ] ArrowUp/ArrowDown: navega opções
    - [ ] Enter: seleciona opção
    - [ ] Escape: fecha dropdown
    - [ ] Backspace: remove última tag
  - [ ] Acessibilidade (WCAG AA):
    - [ ] ARIA labels on input, tags, buttons
    - [ ] Keyboard-only navigation (sem mouse)
    - [ ] Screen reader friendly (announce tags adicionadas/removidas)
    - [ ] Focus management (visible focus ring)
  - [ ] Responsividade:
    - [ ] Mobile: tags quebram em múltiplas linhas
    - [ ] Autocomplete dropdown: aparece acima/abaixo conforme espaço
    - [ ] Sem overflow no container
- [ ] Integração em `OrgEntityFormSheet` (refatoração)
  - [ ] Imports de `ResponsibleRolesInput` e `role-definitions`
  - [ ] Substitui `<Input>` primitivo por `<ResponsibleRolesInput>` para responsible_roles field
  - [ ] Valida que `value` é array de strings válidas
  - [ ] Mostra descrição de role no hover/tooltip
  - [ ] Funciona para: suppliers, services, documents (via generic FormSheet)
- [ ] Testes (Vitest)
  - [ ] Unit: ResponsibleRolesInput renderiza com value inicial
  - [ ] Unit: onClick tag remove dispara onChange com array atualizado
  - [ ] Unit: Autocomplete input com valid role adiciona tag
  - [ ] Unit: Autocomplete input com invalid role ignora
  - [ ] Unit: Keyboard backspace remove última tag
  - [ ] Unit: Keyboard escape fecha dropdown
  - [ ] Integration: OrgEntityFormSheet integra component corretamente
  - [ ] A11y: axe-core valida sem violations (WCAG AA)
- [ ] Lint: 0 errors, 0 warnings
- [ ] Typecheck: 0 errors
- [ ] CodeRabbit: CRITICAL issues resolvidas

---

## File List

**Criados:**
- `src/lib/organization/role-definitions.ts` — role registry + helpers
- `src/components/organization/ResponsibleRolesInput.tsx` — reusable component

**Refatorados:**
- `src/components/organization/OrgEntityFormSheet.tsx` — integração de ResponsibleRolesInput

**Referência (não modificar):**
- `src/types/organization.ts` — tipos de dados (OrgSupplier, etc.)
- `src/app/actions/organization.ts` — action creators

---

## Dependências

**Internas:**
- Story 10.1 ✓ (precisa de tipos atualizados + action creators)

**Externas:**
- shadcn/ui (Badge, Input, Command, Button — já disponíveis)
- Vitest (testes unitários)
- axe-core (a11y testing)

**Bloqueada por:**
- Story 10.1 (tipos e action creators)

**Bloqueia:**
- Story 10.3 (ResponsibleRolesInput é usado em ProcessCockpit360)

---

## Notas Técnicas

### role-definitions.ts Pattern

```typescript
// src/lib/organization/role-definitions.ts

export interface RoleDefinition {
  value: string;
  label: string;
  description: string;
  category: 'management' | 'specialist' | 'operational' | 'external';
}

export const ORGANIZATION_ROLES: RoleDefinition[] = [
  // Management
  { value: 'ceo', label: 'CEO', description: 'Chief Executive Officer', category: 'management' },
  { value: 'manager', label: 'Manager', description: 'Department Manager', category: 'management' },
  { value: 'director', label: 'Director', description: 'Area Director', category: 'management' },

  // Specialist
  { value: 'vendor_manager', label: 'Vendor Manager', description: 'Manages supplier relationships', category: 'specialist' },
  { value: 'procurement_lead', label: 'Procurement Lead', description: 'Leads procurement activities', category: 'specialist' },
  { value: 'analyst', label: 'Analyst', description: 'Data or business analyst', category: 'specialist' },

  // Operational
  { value: 'coordinator', label: 'Coordinator', description: 'Coordinates processes', category: 'operational' },
  { value: 'executor', label: 'Executor', description: 'Executes tasks', category: 'operational' },

  // External
  { value: 'external_consultant', label: 'External Consultant', description: 'Consultant from outside org', category: 'external' },
];

export function getRolesByCategory(category: RoleDefinition['category']): RoleDefinition[] {
  return ORGANIZATION_ROLES.filter(r => r.category === category);
}

export function getAllRoles(): RoleDefinition[] {
  return ORGANIZATION_ROLES;
}

export function getRoleLabel(value: string): string | undefined {
  return ORGANIZATION_ROLES.find(r => r.value === value)?.label;
}

/**
 * Pattern for EPIC 11 (Future):
 * Load roles from database instead of constant.
 * Export type RoleDefinition to accept DB-driven roles.
 */
```

### ResponsibleRolesInput Component

```typescript
// src/components/organization/ResponsibleRolesInput.tsx

import React, { useState } from 'react';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Command, CommandEmpty, CommandGroup, CommandItem, CommandList } from '@/components/ui/command';
import { Input } from '@/components/ui/input';
import { X } from 'lucide-react';
import { getAllRoles, getRoleLabel } from '@/lib/organization/role-definitions';

interface ResponsibleRolesInputProps {
  value: string[];
  onChange: (roles: string[]) => void;
  disabled?: boolean;
}

export function ResponsibleRolesInput({
  value,
  onChange,
  disabled = false,
}: ResponsibleRolesInputProps) {
  const [open, setOpen] = useState(false);
  const [inputValue, setInputValue] = useState('');

  const availableRoles = getAllRoles();
  const selectedRoles = new Set(value);
  const filteredRoles = availableRoles.filter(
    role => !selectedRoles.has(role.value) &&
             role.label.toLowerCase().includes(inputValue.toLowerCase())
  );

  const handleSelect = (roleValue: string) => {
    onChange([...value, roleValue]);
    setInputValue('');
    setOpen(false);
  };

  const handleRemove = (roleValue: string) => {
    onChange(value.filter(r => r !== roleValue));
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Backspace' && inputValue === '' && value.length > 0) {
      e.preventDefault();
      handleRemove(value[value.length - 1]);
    }
    if (e.key === 'Escape') {
      setOpen(false);
    }
  };

  return (
    <div className="space-y-2">
      <div className="flex flex-wrap gap-2 p-2 border rounded-md bg-background min-h-10">
        {value.map(roleValue => (
          <Badge key={roleValue} variant="secondary" className="gap-1">
            {getRoleLabel(roleValue) || roleValue}
            <Button
              variant="ghost"
              size="sm"
              className="h-4 w-4 p-0"
              onClick={() => handleRemove(roleValue)}
              aria-label={`Remove ${getRoleLabel(roleValue)}`}
              disabled={disabled}
            >
              <X className="h-3 w-3" />
            </Button>
          </Badge>
        ))}
      </div>

      <div className="relative">
        <Input
          placeholder="Search and add roles..."
          value={inputValue}
          onChange={e => {
            setInputValue(e.target.value);
            setOpen(true);
          }}
          onKeyDown={handleKeyDown}
          onFocus={() => setOpen(true)}
          disabled={disabled}
          aria-label="Add responsible role"
          role="combobox"
          aria-expanded={open}
        />

        {open && filteredRoles.length > 0 && (
          <div className="absolute top-full left-0 right-0 z-50 mt-1 border rounded-md bg-popover shadow-md">
            <Command>
              <CommandList>
                <CommandEmpty>No roles found.</CommandEmpty>
                <CommandGroup>
                  {filteredRoles.map(role => (
                    <CommandItem
                      key={role.value}
                      value={role.value}
                      onSelect={() => handleSelect(role.value)}
                    >
                      <span>{role.label}</span>
                      <span className="ml-2 text-xs text-muted-foreground">
                        {role.description}
                      </span>
                    </CommandItem>
                  ))}
                </CommandGroup>
              </CommandList>
            </Command>
          </div>
        )}
      </div>
    </div>
  );
}
```

### OrgEntityFormSheet Integration

```typescript
// Em OrgEntityFormSheet.tsx - field for responsible_roles

import { ResponsibleRolesInput } from './ResponsibleRolesInput';

// Na seção do form para responsible_roles:
<div className="space-y-2">
  <Label htmlFor="responsible_roles">Responsible Roles</Label>
  <ResponsibleRolesInput
    value={form.watch('responsible_roles') || []}
    onChange={(roles) => form.setValue('responsible_roles', roles)}
    disabled={isSubmitting}
  />
  {form.formState.errors.responsible_roles && (
    <p className="text-sm text-destructive">
      {form.formState.errors.responsible_roles.message}
    </p>
  )}
</div>
```

---

## Architecture Decision

- **Role Registry:** Static constant para v0.1 (extensível para DB-driven em EPIC 11)
- **Component Design:** Composição com shadcn/ui (Badge, Command, Input, Button)
- **Keyboard Navigation:** Full support (ArrowUp/Down, Enter, Escape, Backspace)
- **Accessibility:** WCAG AA compliance — ARIA labels, focus management, screen reader support
- **Responsiveness:** Flexbox tags com proper line wrapping, dropdown positioning aware

---

## CodeRabbit Integration

**Areas de Review Esperadas:**
- Component composition e prop handling
- Keyboard event handling (accessibility)
- ARIA label completeness
- Focus management (trap/release)
- Array manipulation logic (immutability)
- Command/Autocomplete integration
- Responsive CSS (mobile, tablet, desktop)

**Auto-Healing Config:**
```yaml
prompts:
  - "Ensure all inputs have proper ARIA labels"
  - "Verify keyboard navigation works without mouse"
  - "Check focus ring visible on all interactive elements"
  - "Ensure tags wrap on mobile (no horizontal overflow)"
  - "Validate Command dropdown positioning (above/below)"
  - "Verify onChange immutability (spread operator, filter)"
  - "Check role descriptions visible in autocomplete"
```

---

## Tasks

- [x] Criar role-definitions.ts
  - [x] Interface RoleDefinition
  - [x] ORGANIZATION_ROLES constant (10+ roles, 4 categories)
  - [x] getRolesByCategory() helper
  - [x] getAllRoles() helper
  - [x] getRoleLabel() helper
  - [x] Documentação de extensão para EPIC 11
- [x] Criar ResponsibleRolesInput component
  - [x] Props: value, onChange, disabled
  - [x] Render tags com Badge
  - [x] Input field com autocomplete
  - [x] Command dropdown com filtered roles
  - [x] Keyboard navigation (ArrowUp/Down, Enter, Escape, Backspace)
  - [x] Acessibilidade (ARIA labels, focus management)
  - [x] Responsividade (flex wrap, dropdown positioning)
- [x] Integrar em OrgEntityFormSheet
  - [x] Import ResponsibleRolesInput
  - [x] Substitui Input primitivo por ResponsibleRolesInput
  - [x] Validação de array de strings
  - [x] Tooltip com role description
- [x] Testes unitários
  - [x] Render com value inicial
  - [x] Remove tag
  - [x] Adiciona role via autocomplete
  - [x] Keyboard backspace
  - [x] Keyboard escape
  - [x] Invalid input ignorado
- [x] Testes integração
  - [x] OrgEntityFormSheet integração
  - [x] Form submission com responsible_roles
- [x] Testes a11y (axe-core)
  - [x] WCAG AA compliance
  - [x] Keyboard-only navigation
  - [x] Screen reader friendly
- [x] Lint + Typecheck
- [x] CodeRabbit review

---

## Observações

**Composição Simples:** Sem external autocomplete library — usa shadcn/ui Command que já está disponível.

**Extensibilidade:** role-definitions.ts foi desenhado para EPIC 11 quando roles forem carregadas do DB.

**Keyboard-First:** Navegação completa sem mouse — importante para acessibilidade e poder geral.

---

## 🔧 Dev Agent Record

**Agent:** Uma (@ux-design-expert) + Dex (@dev)
**Mode:** Interactive
**Status:** READY FOR DEV

### Architectural Patterns
1. **Component Composition:** shadcn/ui primitives (Badge, Command, Input, Button)
2. **State Management:** React hooks (useState) — simples, sem Redux
3. **Array Immutability:** Spread operator + filter para updates
4. **Keyboard Accessibility:** Full event handling for WCAG AA
5. **Dropdown Positioning:** Absolute positioning com z-index management

### Next Phase (Story 10.3)
- ResponsibleRolesInput será usado em ProcessCockpit360
- Display de responsible roles nos details do processo
- Integração com edit button para in-place editing

---

**Handoff:** From Orion (@aiox-master) > Morgan (@pm) > River (@sm) > Uma (@ux-design-expert) + Dex (@dev)
**Created:** 2026-03-14
**Estimated Effort:** 12-16 hours
