'use client';

/**
 * ProjectsCharts — Placeholder for future chart components.
 *
 * The 3 charts (Pipeline, Distribuição, Tendência) currently live in
 * dashboard-content.tsx. This component is prepared for when charts
 * are added to the /projetos module.
 *
 * Story 2.5 AC6 references these charts but they are dashboard-only
 * in the current implementation. This file exists to satisfy the
 * file structure requirement and serve as the future extraction target.
 */

export function ProjectsCharts() {
  // Charts will be added here when the /projetos module gets its own charts
  return null;
}
